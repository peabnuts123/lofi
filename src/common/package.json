{
  "name": "@lopoly/common",
  "version": "0.1.0",
  "private": true,
  "description": "Common code for LoPoly projects",
  "author": "peabnuts123",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "publish": "echo '@lopoly/common cannot be published :)'",
    "test": "echo '@lopoly/common has no tests :)'",
    "lint": "eslint --max-warnings 0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --build --noEmit",
    "checkall": "npm run typecheck && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "rimraf": "^6.1.3",
    "tsc-alias": "^1.8.16"
  }
}
