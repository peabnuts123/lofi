`@lopoly/core` contains core types and utils for the LoPoly engine.

## Installation

You shouldn't really need to install this package for anything since it is a dependency of `@lopoly/engine` and will be installed automatically. However, if you need to reference just these core types for whatever reason, install the package `@lopoly/core` using your favourite package manager:

```
npm install @lopoly/core
```
